{
  "packages": ["packages/*", "apps/*", "workers/*"],
  "npmClientArgs": ["--no-lockfile"],
  "version": "independent",
  "command": {
    "version": {
      "ignoreChanges": ["*.md"],
      "npmClient": "pnpm",
      "message": "chore(release): publish"
    },
    "publish": {
      "npmClient": "pnpm"
    }
  }
}
