toc:
  - name: Generating & parsing Georeference Annotations
    children:
      - parseAnnotation
      - generateAnnotation
  - name: Types
    description: |
      The two types this module converts between, [Annotation](#annotation), [AnnotationPage](#annotationpage) and [Map](#map), are described below.
    children:
      - Map
      - Annotation
      - AnnotationPage
