{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://allmaps.org/annotation/schemas/annotation-page.json",
  "title": "Annotation Page with one or more IIIF georeference annotations",
  "type": "object",
  "properties": {
    "type": {
      "const": "AnnotationPage"
    },
    "@context": {
      "$ref": "https://allmaps.org/annotation/schemas/annotation.json#/$defs/context"
    },
    "items": {
      "type": "array",
      "items": {
        "$ref": "https://allmaps.org/annotation/schemas/annotation.json"
      }
    }
  },
  "required": ["type", "items"],
  "errorMessage": "Invalid annotation. Annotation Page must have a property `type` with value `\"AnnotationPage\"` and the property `items` must be an array with 0 or more georeference annotations."
}
