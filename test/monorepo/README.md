# Monorepo tests

This test package contains general tests to assure correct functioning of the Allmaps monorepo.

## Usage

Run them using:

```bash
pnpm run _test
```

Note: The underscore is to avoid conflict with the root `test` script that runs all tests in the monorepo.
